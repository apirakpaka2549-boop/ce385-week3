โครงการนี้ประกอบด้วยสคริปต์ JavaScript สำหรับจัดการข้อมูลและคำนวณผลการเรียนของนักศึกษา แบ่งออกเป็น 3 ส่วนหลัก ดังนี้

1. ระบบคำนวณคะแนนและตัดเกรด (Student Grading)
โมดูลสำหรับรับคะแนนเก็บ/สอบ และสรุปผลการเรียนรายบุคคล

calculateTotalScore(lab, midterm, final): Arrow Function สำหรับรวมคะแนนจากทั้ง 3 ส่วน

assignGrade(totalScore): Function Expression สำหรับแปลงคะแนนรวมเป็นเกรด

คะแนน ≥80→ A

คะแนน 70−79→ B

คะแนน 60−69→ C

คะแนน 50−59→ D

คะแนน <50→ F

createStudentReport(name, lab, midterm, final): Function Statement สำหรับสร้างข้อความสรุปผล (ชื่อ, คะแนนรวม, เกรด, สถานะ ผ่าน/ไม่ผ่าน)

ตัวอย่างการส่งออกผลลัพธ์:

Plaintext
ฟลุค — คะแนนรวม: 85 (เกรด A) — ผ่าน
ฟลุคงับ — คะแนนรวม: 40 (เกรด F) — ไม่ผ่าน
อภิรักษ์ — คะแนนรวม: 70 (เกรด B) — ผ่าน
2. ระบบจัดการรายชื่อนักศึกษา (Student Data Management)
โมดูลสำหรับจัดการข้อมูลนักศึกษาในรูปแบบ Array of Objects โดยเน้นการประมวลผลแบบ Immutable

addStudent(studentList, newStudent): เพิ่มข้อมูลนักศึกษาใหม่ลงใน Array ด้วย Spread Operator (...)

findStudentById(studentList, id): ค้นหาข้อมูลนักศึกษาตามรหัส ID

updateScore(studentList, id, newScore): อัปเดตคะแนนของนักศึกษาตาม ID ที่กำหนดโดยใช้ .map()

โครงสร้างข้อมูลนักศึกษา (Student Object):

JavaScript
{ id: '6501', name: 'ฟลุค', score: 85 }
3. ระบบวิเคราะห์และกรองข้อมูล (Data Filtering & Analytics)
โมดูลสำหรับตรวจสอบและวิเคราะห์คะแนนรวมของชั้นเรียนด้วย Array High-Order Functions

.filter(): คัดกรองเฉพาะนักศึกษาที่ได้คะแนนสอบ ≥50

.map(): แปลงข้อมูลรายชื่อที่ผ่านการกรองให้อยู่ในรูปแบบข้อความรายงาน

.every(): ตรวจสอบว่านักศึกษาทุกคนในชั้นเรียนสอบผ่านหรือไม่ (≥50)

.some(): ตรวจสอบว่ามีนักศึกษาที่ได้คะแนนวิกฤต (<40) หรือไม่